# SPACE STRIKE

Juego HTML5 — acceso anticipado / beta.

## Jugar

Abre: **GitHub Pages** → `https://TU_USUARIO.github.io/NOMBRE-REPO/`

O abre `index.html` en un servidor local (no uses `file://` en el celular si falla el JS).

## Estructura

```
index.html
css/
html/
javascript/
img/
```

## Notas

- Progreso y ranking: Firebase (config en el código cliente + reglas de seguridad).
- La carpeta `netlify/` solo aplica si despliegas en Netlify; en GitHub Pages no se usa.
- Versión actual: ver etiqueta en el menú del juego.
